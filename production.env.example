# Production Environment Variables for Railway
# Copy these to Railway's environment variables section

# Database Configuration
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/chatter_analytics?retryWrites=true&w=majority
# Alternative: Use Railway's MongoDB (automatically provides MONGO_URL)
# MONGO_URL= (Railway will set this automatically if you add MongoDB service)

# OpenAI Configuration
OPENAI_API_KEY=sk-your-openai-api-key-here

# Application Configuration
NODE_ENV=production
PORT=5000

# Optional: Add custom domain
# DOMAIN=your-custom-domain.com

# Optional: Add security settings
# JWT_SECRET=your-jwt-secret-for-future-auth
# SESSION_SECRET=your-session-secret
